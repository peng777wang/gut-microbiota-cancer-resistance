# 上传说明（v1.0.3）—— 2026-09-23

本文件夹里的 5 个文件要在 GitHub 上**覆盖**同名文件（路径必须保持）：

```
README.md
CITATION.cff
data_derived/software_versions_bertopic.txt
docs/supplementary_methods_d3tem.docx
docs/FILE_MANIFEST.csv
```

## 1. 上传到 GitHub（网页版）

1. 打开 <https://github.com/peng777wang/gut-microbiota-cancer-resistance>，
   确认当前分支是 **main**（默认分支）。
2. 点右上方的 **Add file → Upload files**。
3. 在资源管理器里打开本文件夹，把 **README.md、CITATION.cff、data_derived 文件夹、docs 文件夹**
   一起拖进上传区。
   - 一定要拖 `data_derived` 和 `docs` 这两个**文件夹**（而不是文件夹里面的文件），
     否则文件会落到仓库根目录。
4. 等文件列表出现（应显示 5 个文件，其中两个前任文件夹路径会自动带出来）。
5. 下方 **Commit changes**：
   - Message 填：`v1.0.3: software version report completed (POT 0.9.7.post1); D3TEM supplement and metadata updated`
   - 选 **Commit directly to the main branch** → 点 **Commit changes**。

## 2. 打新 release

1. 仓库首页右侧 **Releases → Create a new release**。
2. **Choose a tag** 输入 `v1.0.3` → 点 **Create new tag: v1.0.3 on publish**。
3. Release title：`Reproducibility package v1.0.3`
4. 说明（可粘贴）：
   > Matches the revised manuscript: completed software version report (hdbscan 0.8.44, POT
   > 0.9.7.post1, statsmodels 0.15.0), updated D3TEM supplementary methods, and the updated
   > package checksum manifest. Also updates the repository title to
   > "a bibliometric study of gut microbiota research in cancer therapy resistance (2016 - 2025)".
5. 点 **Publish release**。

## 3. Zenodo 取新版本 DOI

1. <https://zenodo.org> → 右上 **Sign in with GitHub**（以前链过一次，应已授权）。
2. 右上头像 → **Settings → GitHub**：确认
   `peng777wang/gut-microbiota-cancer-resistance` 的开关是 **ON**
   （v1.0.2 已归档过，所以通常已经是 ON）。
3. 回到 **My dashboard / Uploads**，等 1–2 分钟，会出现新记录
   `Reproducibility package v1.0.3`（标题应已是新题名）。
   若没出现：Settings → GitHub → 该仓库右侧点 **Sync now**。
4. 打开新记录，复制 **版本 DOI**（形如 `10.5281/zenodo.XXXXXXXX`；
   记录页同时显示 concept DOI，请以版本 DOI 为准）。
5. 把该 DOI 发给我。

## 4. 我这边随后会做

- 把手稿 Data Availability 里的 DOI 换成新的版本 DOI（p. 31, lines 1331–1343 那一段）；
- 把 README 里 "the version accompanying the manuscript is v1.0.3, archived under its own
  version DOI" 换成具体 DOI；
- 重导手稿 PDF → 重建行号索引 → 重跑回复信全部引用校准 → 输出最终 PDF。
